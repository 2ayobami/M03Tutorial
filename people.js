const people = ['Yoshi', 'Ryu', 'Chun-li', 'Mario'];
const ages = [20, 25, 30, 35]

console.log(people);

// Exporting the value of module.exports to modules.js file
module.exports = {
    people, ages
};

